# Swap Tool — Dependencies
# Cài đặt: pip install -r requirements.txt

# Deep-Live-Cam core
numpy>=1.23.5,<2
opencv-python==4.10.0.84
onnx==1.18.0
insightface==0.7.3
psutil==5.9.8
pillow>=10.0.0
protobuf==4.25.1
tqdm

# GPU NVIDIA (bỏ comment nếu dùng GPU)
# onnxruntime-gpu==1.24.2

# CPU only
onnxruntime>=1.16.0
